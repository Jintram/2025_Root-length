[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "root-length"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["custom_functions*", "functions_files*", "functions_pipeline*"]



