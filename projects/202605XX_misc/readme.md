

Not entirely sure about the history of `pipelineclean_phase3_example_roots.py`,
I think I copied this from `cheeky-cells` repo to build further, 
and it is also the layout how a previous model was used for segmentation
(`modelUNet20251026_1027.pth`)

(Also not sure about the date of that I gave to this subfolder, 
but I know it's a bit earlier than 2026-06-20.)