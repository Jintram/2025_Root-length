
This test-runs the model that was trained on both annotations by
Yuzeng (high-res only) and Martijn (incl low-res).

The data by Yuzeng was cropped, such that only plants and "pure" agar
were trained on, the dish and black background were never shown to the 
model.

Model: `modelUNet20260619_2100__trained0d19h46m.pth`