# Preliminary script

Proof-of-principle script that runs through an analysis of one image, but 
is not scalable.

I used this script as basis to create the scripts in the `pipeline_functions`
folder.